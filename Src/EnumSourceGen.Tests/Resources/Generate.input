//This test checks if Generate is read correctly

namespace Some.Namespace.Here;

[EnumSourceGen(Generate = Generate.ClassOnly)]
public enum ClassOnlyEnum
{
    ClassOnly
}

[EnumSourceGen(Generate = Generate.ExtensionsOnly)]
public enum ExtensionsOnlyEnum
{
    ExtensionsOnly
}