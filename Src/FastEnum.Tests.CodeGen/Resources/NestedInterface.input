// Ensure enums nested inside interfaces are supported.

namespace Some.Namespace.Here;

public interface IMyInterface
{
    [FastEnum]
    public enum MyEnum
    {
        First,
        Second
    }
}
